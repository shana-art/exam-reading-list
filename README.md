# Exam – Reading List

Detta repository innehåller automatiserade tester för applikationen **Reading List**.

Projektet använder:
- Behave (BDD)
- Playwright
- Pytest (smoke test)

## Installation

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
playwright install

